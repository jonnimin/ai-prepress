# AI 印前助手 V7 功能規格書

## 1. V7 產品目標

V7 的核心不是單純轉檔，而是「先檢查，再修復」的印前流程工具。

使用者流程：

```text
Step 1：選品項與尺寸
Step 2：上傳檔案
Step 3：線上檔案檢查
Step 4：依檢查結果選修復功能
```

V7 要解決的問題：

```text
1. 客戶不知道檔案能不能印。
2. 客戶不知道要不要出血。
3. 客戶不知道解析度是否足夠。
4. 客戶不知道 RGB / CMYK、文字、裁切線是否有問題。
5. 客戶不知道應該用哪個修復功能。
```

所以 V7 的設計邏輯是：

```text
檔案上傳 → 自動檢查 → 產生風險報告 → 推薦修復功能 → 下載處理檔 / 轉人工完稿
```

---

## 2. Step 1：選品項與尺寸

### 2.1 品項

目前 V7 內建：

```text
一般名片：9 x 5.4 cm
一般 A4DM：21 x 29.7 cm
一般 A3DM：29.7 x 42 cm
一般貼紙：9 x 5.4 cm
明信片：10.5 x 14.8 cm
自訂尺寸：使用者輸入寬 x 長 cm
```

### 2.2 直式 / 橫式

每個品項可以切換：

```text
橫式
直式
```

程式行為：

```text
橫式：寬取較長邊，高取較短邊
直式：寬取較短邊，高取較長邊
```

### 2.3 出血值

可選：

```text
0cm：不出血
0.2cm：2mm
0.3cm：3mm
0.5cm：5mm
1cm：10mm
```

範例：

```text
成品尺寸：9 x 5.4 cm
出血：0.2 cm
出血後尺寸：9.4 x 5.8 cm
```

---

## 3. Step 2：上傳檔案

支援上傳：

```text
JPG
PNG
WebP
PDF
AI
EPS
SVG
```

目前可直接處理：

```text
JPG
PNG
WebP
```

需進一步預檢或人工：

```text
PDF
AI
EPS
SVG
```

原因：

```text
PDF 可能含字型、透明度、裁切線、出血、色彩模式。
AI / EPS / SVG 可能含缺字型、連結圖、特效、未轉曲文字。
```

---

## 4. 方案 A：前端預覽框線

V7 已採用方案 A，先做前端預覽，不做真正 AI 補邊。

### 4.1 預覽層級

預覽畫面包含：

```text
底圖：使用者圖檔或品項示意圖
最外層紅框：出血尺寸
中間黑框：成品裁切尺寸
內層藍色虛線：安全範圍
四角十字線：對位 / 裁切標
```

### 4.2 預覽目的

讓使用者在上傳前就理解：

```text
哪裡是出血區
哪裡是裁切區
哪裡是安全範圍
文字或 Logo 是否太靠邊
```

### 4.3 注意事項

前端預覽只做視覺輔助，不代表最終輸出結果。

正式輸出仍需後端產 PDF。

---

## 5. Step 3：線上檔案檢查

### 5.1 API

```text
api/preflight_check.php
```

### 5.2 檢查項目

圖片檔可檢查：

```text
檔案格式
檔案大小
圖片像素尺寸
目標印刷尺寸
估算 DPI
尺寸比例差異
是否設定出血
是否建議轉 CMYK
建議修復功能
風險等級
```

PDF / AI / EPS / SVG 目前回傳預檢建議：

```text
PDF：建議檢查字型、出血、裁切線、色彩模式
AI / EPS / SVG：建議檢查字型、連結圖、透明效果、文字轉外框
```

### 5.3 風險等級

```text
低風險：DPI 足夠、比例接近
中風險：需要出血 / CMYK / 人工確認
高風險：解析度太低、比例差太多、不支援格式
```

---

## 6. Step 4：依檢查結果選修復功能

### 6.1 一般功能

```text
轉印刷檔
加出血
出血 + 裁切線
轉 CMYK
放大 200%
亮P / 霧P 黑版
```

### 6.2 VIP 功能

```text
AI 智慧出血延伸
文字轉外框
300% / 400% 放大
進階 PDF 預檢
```

### 6.3 人工完稿

以下情況建議人工：

```text
複雜 AI 檔
缺字型
多頁 PDF
特殊刀模
燙金 / 白墨 / 局部光
包裝盒展開圖
文字或 Logo 太靠邊
AI 補邊結果不自然
```

---

## 7. AI 智慧出血延伸

### 7.1 正確邏輯

以名片為例：

```text
成品尺寸：9 x 5.4 cm
出血值：0.2 cm
出血後尺寸：9.4 x 5.8 cm
```

處理邏輯：

```text
1. 原圖保持在 9 x 5.4 cm 成品區。
2. 四周新增 0.2 cm 出血區。
3. 出血區由 AI outpainting 生成。
4. 預覽時疊加出血框、裁切框、安全線與十字對位標。
5. 最後輸出 9.4 x 5.8 cm PDF。
```

### 7.2 V7 現況

V7 已完成：

```text
前端預覽框線
出血尺寸計算
成品框 / 出血框 / 安全框 / 十字對位標
```

V7 尚未完成：

```text
真正 AI outpainting 補邊
```

### 7.3 下一階段 API

建議新增：

```text
api/ai_bleed_extend.php
```

後端流程：

```text
1. 讀取圖片
2. 建立出血後畫布
3. 原圖置中於成品區
4. 建立 mask，mask 區域為四周出血區
5. 呼叫 AI outpainting 模型
6. 回傳補邊後圖片
7. 轉 PDF 並加入裁切線
```

可接：

```text
ComfyUI API
Stable Diffusion / SDXL outpainting
商用圖片生成 API
Photoshop 類型生成式填色服務
```

---

## 8. 文字轉外框

### 8.1 功能定位

建議列為 VIP 功能。

前台名稱：

```text
文字轉外框
避免缺字型、跑版、亂碼
```

### 8.2 可處理檔案

```text
PDF：可嘗試用 Ghostscript
SVG：可嘗試用 Inkscape
AI / EPS：建議人工完稿
```

### 8.3 PDF 文字轉外框建議指令

```bash
gs -o output_outline.pdf -sDEVICE=pdfwrite -dNoOutputFonts input.pdf
```

### 8.4 SVG 文字轉路徑建議指令

```bash
inkscape input.svg --export-text-to-path --export-filename=output.pdf
```

### 8.5 風險

```text
PDF 可能變大
透明效果可能展平
特效可能跑版
部分字型可能無法完全轉換
```

所以不能寫「保證所有檔案自動轉外框」。

---

## 9. 免費 / VIP / 人工完稿分級

### 免費

```text
線上檔案檢查
基本轉 PDF
基本出血預覽
裁切線預覽
前端風險提示
```

### 一般會員

```text
轉印刷檔
加出血
出血 + 裁切線
轉 CMYK
放大 200%
亮P / 霧P 黑版草稿
```

### VIP

```text
AI 智慧出血延伸
文字轉外框
300% / 400% 放大
進階 PDF 預檢
更多使用次數
批次處理
```

### 人工完稿

```text
特殊刀模
燙金
白墨
局部光
包裝盒展開圖
複雜 AI / EPS / PDF
字型與版面確認
```

---

## 10. V7 已建立檔案

```text
index.html
skin/css/style.css
skin/js/main.js
api/preflight_check.php
api/outline_text.php
api/process.php
V7功能規格書.md
```

---

## 11. 下一階段建議

V8 建議做：

```text
1. 真正 AI 智慧出血延伸 API
2. PDF 字型檢查 pdffonts
3. PDF 文字轉外框 Ghostscript
4. SVG 文字轉路徑 Inkscape
5. 使用者會員與使用次數資料庫
6. 上傳紀錄與下載紀錄
7. 人工完稿報價表單
```


---

## V8 修正補充

### Step 1 出血欄位

V8 將 Step 1 的出血設定簡化為單一選單，只允許：

```text
出血 0.2CM
出血 0.3CM
```

不再額外顯示數字輸入欄，避免使用者混淆。

### 直式 / 橫式

V8 移除 Step 1 的直式 / 橫式，改放到 Step 2 預覽區。  
使用者可在預覽檔案時切換直式 / 橫式，預覽框會同步更新。

### 預覽框線修正

V8 的預覽框線改為依實際尺寸動態計算：

```text
出血框 = 成品尺寸 + 左右上下出血
裁切框 = 成品尺寸
安全框 = 裁切框內縮
十字對位標 = 對齊裁切框四角
```

以名片 9 x 5.4cm、出血 0.2cm 為例：

```text
裁切框：9 x 5.4cm
出血框：9.4 x 5.8cm
```

十字對位標會落在 9 x 5.4cm 的裁切框四角，不再錯位。


---

## V9 預覽標示規格

依照印刷廠常見示意圖，預覽標示改為：

```text
桃紅線：出血尺寸
藍色虛線：完成尺寸 / 成品裁切尺寸
灰色十字：四角對位標 / 裁切標記
```

十字對位標必須標在完成尺寸四個角，而不是出血框四個角。

預覽圖要讓使用者能清楚分辨：

```text
1. 圖檔成品完成尺寸在哪裡
2. 四周多出來的出血區在哪裡
3. 裁切 / 對位標在哪裡
```
